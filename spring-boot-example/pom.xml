<?xml version="1.0" encoding="UTF-8"?>
<!--
  deepseek-harness4j-spring-boot-example: runnable Spring Boot MVC application demonstrating
  the starter. POST /api/harness/run runs one agent turn over a Spring MVC controller;
  Spring Cloud Config (when present) can supply the deepseek.harness.* properties.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.deepseek-ai</groupId>
    <artifactId>deepseek-harness4j</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>deepseek-harness4j-spring-boot-example</artifactId>
  <name>deepseek-harness4j-spring-boot-example</name>
  <description>Runnable Spring Boot MVC example over the DeepSeek Harness Java SDK</description>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring.boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>com.deepseek-ai</groupId>
      <artifactId>deepseek-harness4j-spring-boot-starter</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring.boot.version}</version>
      </plugin>
    </plugins>
  </build>
</project>
